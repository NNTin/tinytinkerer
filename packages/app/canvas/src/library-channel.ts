// Shared by the tiny callback entry and the live canvas stage. Kept in a leaf
// module so the callback does not load React, Excalidraw, or the tool schemas.
export const EXCALIDRAW_LIBRARY_CHANNEL = 'tinytinkerer:canvas-library'
