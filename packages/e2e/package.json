{
  "name": "@tinytinkerer/e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Playwright end-to-end tests. Real-browser verification of the code-exec sandbox isolation guarantees (issue #217). Not part of the vitest unit-test run.",
  "scripts": {
    "e2e": "node ./scripts/e2e.mjs",
    "e2e:install": "playwright install --with-deps chromium",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "allure-playwright": "3.10.0"
  }
}
