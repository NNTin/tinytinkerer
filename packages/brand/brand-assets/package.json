{
  "name": "@tinytinkerer/brand-assets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "pnpm -w generate:brand-assets && pnpm -w generate:notices",
    "build": "pnpm -w generate:brand-assets && pnpm -w generate:notices && tsc -p tsconfig.json",
    "lint": "eslint src tests",
    "test": "pnpm -w generate:brand-assets && pnpm -w generate:notices && vitest run",
    "typecheck": "pnpm -w generate:brand-assets && pnpm -w generate:notices && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@tinytinkerer/contracts": "workspace:*",
    "react-icons": "5.6.0"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "19.2.15"
  }
}
