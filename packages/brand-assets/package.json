{
  "name": "@tinytinkerer/brand-assets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "pnpm -w generate:brand-assets",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src tests",
    "test": "pnpm -w generate:brand-assets && vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@tinytinkerer/contracts": "workspace:*"
  }
}
