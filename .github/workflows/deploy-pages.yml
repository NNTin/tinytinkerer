name: Deploy Vercel

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: vercel-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
  GIT_SHA: ${{ github.sha }}

jobs:
  checks:
    name: Mermaid, Boundaries & Skill README
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.16.1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: npm install --global @mermaid-js/mermaid-cli@11.12.0

      - name: Mermaid render check
        run: pnpm check:mermaid

      - name: Boundary check
        run: pnpm check:boundaries

      - name: GitHub Models OpenAPI check
        env:
          GITHUB_MODELS_TOKEN: ${{ secrets.GITHUB_MODELS_TOKEN }}
        run: pnpm check:github-models-openapi

      - name: Skill README sync check
        run: pnpm check:skill-readme

  ci:
    name: ${{ matrix.task.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task:
          - name: Lint & Typecheck
            run: pnpm exec turbo run lint typecheck --concurrency=2
          - name: Test
            run: pnpm exec turbo run test --concurrency=1
          - name: Build
            run: pnpm exec turbo run build --concurrency=2
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.16.1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm generate:brand-assets
      - run: pnpm generate:privacy-policy
      - run: TINYTINKERER_SKIP_BRAND_ASSET_GENERATION=1 ${{ matrix.task.run }}

  deploy-production:
    name: Deploy Production
    needs: [checks, ci]
    if: >
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.16.1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=production --token="$VERCEL_TOKEN"

      - name: Build Deployment Artifacts
        # VITE_SENTRY_DSN activates runtime error reporting and SENTRY_AUTH_TOKEN
        # enables source map upload from the Sentry Vite plugin during the build
        # (see apps/web/vite.config.ts). Production only — preview builds ship
        # without a DSN so their errors never reach Sentry.
        env:
          VITE_SENTRY_DSN: ${{ secrets.SENTRY_DSN_FRONTEND }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        run: vercel build --prod --token="$VERCEL_TOKEN"

      - name: Deploy to Vercel
        id: deploy
        run: |
          url="$(vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN")"
          echo "url=$url" >> "$GITHUB_OUTPUT"

      - name: Alias Production Domain
        run: vercel alias set "${{ steps.deploy.outputs.url }}" "tiny.nntin.xyz" --token="$VERCEL_TOKEN"

  deploy-preview:
    name: Deploy Preview
    needs: [checks, ci]
    if: >
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.16.1

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=preview --token="$VERCEL_TOKEN"

      - name: Build Deployment Artifacts
        run: vercel build --token="$VERCEL_TOKEN"

      - name: Deploy to Vercel
        id: deploy
        run: |
          url="$(vercel deploy --prebuilt --token="$VERCEL_TOKEN")"
          echo "url=$url" >> "$GITHUB_OUTPUT"

      - name: Derive Preview Domain
        id: preview-domain
        shell: bash
        run: |
          branch_slug="$(printf '%s' "${{ github.event.pull_request.head.ref }}" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9-]+/-/g; s/-+/-/g; s/^-+//; s/-+$//')"
          if [ -z "$branch_slug" ]; then
            branch_slug="preview"
          fi

          suffix=".tiny.preview.nntin.xyz"
          prefix="pr-${{ github.event.pull_request.number }}-"

          # TLS certificate common names are capped at 64 characters, so the
          # whole domain (prefix + branch + suffix) must fit — truncating only
          # the branch to a fixed length is not enough for long branch names or
          # large PR numbers. Derive the branch budget from what's left after
          # the fixed parts. Target 63 to keep a one-character safety margin
          # under the hard 64-character limit.
          max_domain_length=63
          max_branch_length=$(( max_domain_length - ${#suffix} - ${#prefix} ))
          if [ "$max_branch_length" -lt 1 ]; then
            max_branch_length=1
          fi
          branch_slug="${branch_slug:0:$max_branch_length}"
          branch_slug="${branch_slug%-}"

          slug="${prefix}${branch_slug}"
          slug="${slug%-}"
          domain="$slug$suffix"

          echo "slug=$slug" >> "$GITHUB_OUTPUT"
          echo "domain=$domain" >> "$GITHUB_OUTPUT"

      - name: Alias Preview Domain
        id: alias
        run: |
          domain="${{ steps.preview-domain.outputs.domain }}"
          vercel alias set "${{ steps.deploy.outputs.url }}" "$domain" --token="$VERCEL_TOKEN"
          echo "preview_url=https://$domain" >> "$GITHUB_OUTPUT"

      - name: Comment Preview Link
        uses: actions/github-script@v7
        env:
          PREVIEW_URL: ${{ steps.alias.outputs.preview_url }}
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.url }}
          PREVIEW_DOMAIN: ${{ steps.preview-domain.outputs.domain }}
        with:
          script: |
            const marker = '<!-- vercel-preview -->'
            const issue_number = context.payload.pull_request.number
            const comments = await github.paginate(github.rest.issues.listComments, {
              ...context.repo,
              issue_number,
              per_page: 100,
            })

            const body = [
              marker,
              '## Vercel Preview',
              '',
              `Preview: ${process.env.PREVIEW_URL}`,
              `Deployment: ${process.env.DEPLOYMENT_URL}`,
              `Domain: \`${process.env.PREVIEW_DOMAIN}\``,
              `Commit: \`${process.env.GITHUB_SHA}\``,
            ].join('\n')

            const existingComment = comments.find((comment) =>
              comment.user?.type === 'Bot' && comment.body?.includes(marker)
            )

            if (existingComment) {
              await github.rest.issues.updateComment({
                ...context.repo,
                comment_id: existingComment.id,
                body,
              })
              return
            }

            await github.rest.issues.createComment({
              ...context.repo,
              issue_number,
              body,
            })
