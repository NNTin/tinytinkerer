# git-merge

<!-- BEGIN GENERATED: .agent/README.md — do not edit; run `pnpm sync:skill-readme`

# `.agent` — WAT skills (Workflow · Agent · Tools)

Skills the agent uses to work in this repo. Core idea: **offload deterministic steps to scripts so you stay focused on decisions.** Chained 90%-accurate manual steps decay fast (0.9^5 ≈ 59%) — scripts don't drift, and they save tokens.

## Skill layout

```
.agent/skills/<skill-name>/
  SKILL.md      # when to use, how, available tools, constraints, success criteria
  workflows/    # markdown SOPs (step-by-step procedures)
  tools/        # deterministic scripts the workflows call
```

## How you (the agent) work

1. Match the task to a skill, read its `SKILL.md`.
2. Scan workflow **filenames** for a relevant SOP — don't read every file.
3. Follow the SOP; run the tool scripts instead of doing the steps by hand.
4. **Self-evolve:** if you solved something repeatable the hard way, capture it as a new workflow SOP (+ tool). Future agents thank you.

END GENERATED: .agent/README.md -->

Merge a commit, branch, PR head, or remote ref into the current branch and push
the result safely, using **local `git merge` + `git push`** — never `gh`. Read
`../../README.md` first for the WAT framework.

## When to use

- A user asks to merge a specific commit, branch, PR, or ref into the current
  branch.
- A user asks to resolve merge conflicts and push the result.

This skill always merges locally with `git merge` and publishes with `git push`.
It never routes a merge through a PR (`gh pr merge`) and never uses `gh` to land
commits. If a protected branch rejects the push, surface the rejection and stop;
do not fall back to `gh`.

## How

1. Follow `workflows/merge-and-push.md`.
2. Ensure the husky hooks are installed in the worktree you commit in (the
   workflow's first step) so the merge subject gets rewritten correctly.
3. Treat conflict resolution as judgment work: inspect both sides, preserve
   intended behavior, and keep unrelated changes untouched.
4. Report the merge commit, pushed branch, conflict files, and verification.

## Available tools

No skill-local tool scripts are provided. Merge conflicts cannot be solved
correctly by a generic script; use Git, repo tests, and semantic code review.

## Constraints

- Start with `git status --short --branch`; never overwrite unrelated dirty
  work.
- Merge with plain `git merge <ref>`: fast-forward when the target branch is a
  descendant, otherwise create a merge commit. This is Git's default `--ff`
  behavior — do not change it.
- **Never rebase and never squash.** Do not run `git rebase`, do not pass
  `--rebase`, and do not squash-merge.
- **Never use `gh` to merge.** No `gh pr merge`, no routing merges through PRs.
  Land commits with `git merge` locally and `git push origin <branch>`.
- Install hooks before merging. The repo uses husky v9; its generated shims live
  in `.husky/_/` (gitignored, regenerated by `pnpm install`). Without them the
  `prepare-commit-msg` hook never runs and the merge lands as a raw
  `Merge branch ...` subject. Let the hook rewrite the subject — do not
  hand-write a conventional merge message and do not pass `-m` to set one.
- Do not use destructive commands such as `git reset --hard` or
  `git checkout --` unless the user explicitly asked for that operation.
- Resolve conflicts semantically, not by blindly choosing one side.
- Stage only files that belong to the merge resolution.
- Verify before pushing. If tests cannot be run, say exactly why.
- Never force-push. If a protected branch rejects the push (e.g. GH013,
  "Changes must be made through a pull request"), report it and stop.

## Success criteria

- The requested ref is merged into the intended branch via local `git merge`
  (fast-forward when possible, otherwise a merge commit) — no rebase, no squash,
  no `gh`.
- The husky `prepare-commit-msg` hook rewrote the merge subject to
  `chore(merge): merge <MERGED_COMMIT> into <TARGET_COMMIT>` (not a raw
  `Merge branch ...`).
- All conflicts are resolved and committed.
- Relevant checks/tests have passed or any inability to run them is reported.
- The intended remote branch contains the merge commit, pushed with
  `git push origin <branch>` — or a protected-branch rejection was reported and
  the agent stopped without falling back to `gh`.
- The worktree is clean at handoff.
