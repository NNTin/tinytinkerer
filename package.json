{
  "name": "tinytinkerer",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.16.1",
  "scripts": {
    "generate:brand-assets": "node ./scripts/generate-brand-assets.mjs",
    "generate:privacy-policy": "node ./scripts/generate-privacy-policy.mjs",
    "dev": "pnpm generate:brand-assets && pnpm generate:privacy-policy && TINYTINKERER_SKIP_BRAND_ASSET_GENERATION=1 turbo run dev --filter=@tinytinkerer/edge --filter=@tinytinkerer/host",
    "build": "pnpm generate:brand-assets && pnpm generate:privacy-policy && TINYTINKERER_SKIP_BRAND_ASSET_GENERATION=1 turbo run build",
    "build:pages": "pnpm generate:brand-assets && pnpm generate:privacy-policy && TINYTINKERER_SKIP_BRAND_ASSET_GENERATION=1 TINYTINKERER_DEPLOY_BASE=/tinytinkerer turbo run build",
    "check:boundaries": "node ./scripts/check-boundaries.mjs",
    "check:mermaid": "node ./scripts/check-mermaid.mjs",
    "sync:skill-readme": "node ./scripts/sync-skill-readme.mjs",
    "check:skill-readme": "node ./scripts/sync-skill-readme.mjs --check",
    "lint": "pnpm generate:brand-assets && pnpm generate:privacy-policy && TINYTINKERER_SKIP_BRAND_ASSET_GENERATION=1 turbo run lint && node ./scripts/check-boundaries.mjs",
    "test": "pnpm generate:brand-assets && pnpm generate:privacy-policy && turbo run test",
    "typecheck": "pnpm generate:brand-assets && pnpm generate:privacy-policy && turbo run typecheck",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.12.4",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "png-to-ico": "3.0.1",
    "prettier": "^3.6.2",
    "sharp": "0.34.5",
    "turbo": "^2.5.8",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.4",
    "vitest": "^4.0.8"
  }
}
